server:
  port: 9101

spring:
  application:
    name: config-client
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml # 指定配置文件格式
        # namespace: # 命令空间配置
        # group: # 分组配置