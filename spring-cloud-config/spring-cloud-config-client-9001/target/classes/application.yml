server:
  port: 9001
spring:
  application:
    name: spring-cloud-config-client-9001
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

management:
  endpoints:
    web:
      exposure:
        include: '*'